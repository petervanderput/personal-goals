# Secrets — never commit
.Renviron
.env

# R
.rlib/
.Rproj.user/
.Rhistory
.RData
.Ruserdata

# Quarto / rendered output
/.quarto/
*.html
*_files/
*.knit.md

# The dashboard is generated, but it is also the published artifact, so it is the
# one HTML file that has to be committed.
!docs/index.html

# Scratch logs and local experiments
_*.log
_scratch/
_*.csv
